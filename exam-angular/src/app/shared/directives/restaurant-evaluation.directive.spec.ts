import { RestaurantEvaluationDirective } from './restaurant-evaluation.directive';

describe('RestaurantEvaluationDirective', () => {
  it('should create an instance', () => {
    const directive = new RestaurantEvaluationDirective();
    expect(directive).toBeTruthy();
  });
});
